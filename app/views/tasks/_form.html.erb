<%# @task.errors.full_messages.join(", ") %>
<%= form_for [@project, @task], remote: true do |f| %>
  <div>
    <%= f.label :body %>
    <%= f.text_field :body %>
  </div>
  <%= f.submit %>
<% end %>
