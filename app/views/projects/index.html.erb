<h1>All Projects</h1>

<% @projects.each do |project| %>
  <div>
    <h2><%= link_to project.title, project_path(project) %></h2>
    <p><%= project.description %></p>
    <p class="due_date"><%= project.due_date %></p>
  </div>
<% end %>
